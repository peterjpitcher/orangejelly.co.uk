Comparison table with an ink header band; `highlight` marks the recommended column in orange.

```jsx
<CompareTable
  columns={['Typical agency','Orange Jelly']} highlight={1}
  rows={[
    {label:'Starts from',values:['A deliverables list','Your growth problem']},
    {label:'Tested on a real business first',values:[false,true]},
    {label:'Measured outcome',values:['Impressions','Revenue and margin']}
  ]} />
```

Booleans render ✓/—. Keep cell copy short; the table argues, the prose explains.
