Big evidence number with mono label; always pair bold claims with a qualifier in `sub`.

```jsx
<Stat value="403%" label="table bookings" sub="12 months vs prior year, The Anchor" size="lg" tone="dark" />
```
