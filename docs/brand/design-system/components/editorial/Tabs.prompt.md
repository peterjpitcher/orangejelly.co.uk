```jsx
<Tabs items={[{label:'By pressure point',content:<Grid/>},{label:'By sector',content:<Sectors/>}]} />
```

In-page content switching only (results filters, method detail) — never primary navigation.
