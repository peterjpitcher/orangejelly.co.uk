```jsx
<EmptyState title="No articles match those filters." body="Loosen a filter, or start from the problem instead." action={{label:'Browse growth problems',href:'/growth-problems'}} />
```

Always ship an `action` — an empty state without a route out is a dead end.
