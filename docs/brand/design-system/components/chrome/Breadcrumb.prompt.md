Trail for inner pages; sits directly under the Header, above the page title.

```jsx
<Breadcrumb items={[{label:'Home',href:'/'},{label:'Insights',href:'/insights'},{label:'Why discounting is a margin trap'}]} />
```

Orange → separators; the current page is ink and unlinked. Mirror the same trail in BreadcrumbList structured data.
