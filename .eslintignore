# Ignore all JSON files
*.json
**/*.json
# ...but /feed.json is a route directory, not a JSON file, and its handler must be linted.
!src/app/feed.json
!src/app/feed.json/route.ts

# Ignore config files
next.config.js
tailwind.config.js
postcss.config.js

# Ignore build outputs
.next
out
dist

# Ignore node modules
node_modules

# Ignore coverage
coverage

# Ignore public folder
public
# Vendored brand and design-system handoff: keep byte-identical to delivery
docs/brand/
