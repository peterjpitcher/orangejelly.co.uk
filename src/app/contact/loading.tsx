/**
 * The site loading screen, shown while this page renders on request. See
 * `SiteLoadingScreen` for why it lives per page rather than at the app root.
 */
export { default } from '@/components/SiteLoadingScreen';
