// Transitional wrapper - will be removed after full migration
import ImageAdapter, { ResponsiveImage, OptimizedBackground } from './adapters/ImageAdapter';

export default ImageAdapter;
export { ResponsiveImage, OptimizedBackground };
